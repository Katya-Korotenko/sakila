# наброски запросов
SEARCH_TITLE_QUERY = """
                        select title, description, release_year
                        from film
                        where title like '%DEVIL%';
                     """

